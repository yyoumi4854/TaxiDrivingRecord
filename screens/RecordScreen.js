import { SafeAreaView, ScrollView, Text } from "react-native";

const RecordScreen = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>여기는 운행기록 작성하는 곳</Text>
      </ScrollView>
    </SafeAreaView>
  );
};
export default RecordScreen;
